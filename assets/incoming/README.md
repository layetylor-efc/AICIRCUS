# AICIRCUS incoming images

ここに画像を置いて `node scripts/auto-image-integrator.js` を実行すると、既知の EGO / FATE LOG 素材を自動分類して `assets/icons/` へ配置し、`data/image-manifest.json` を更新します。

推奨ファイル名例:
- `ego-icon.jpg`
- `fatelog-icon.jpg`
